import {makeStyles} from '@material-ui/core/styles';

export default makeStyles(() => ({
    albumsHeader: {
        padding: '30px 0'
    },
    artistName: {
        textTransform: 'capitalize',
    },
}));
