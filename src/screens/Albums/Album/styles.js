import {makeStyles} from '@material-ui/core/styles';

export default makeStyles(() => ({
    albumAction: {
        width: '100%',
        textAlign: 'center',
        textTransform: 'unset',
    },
}));
